name: ckgroup_core_engine
description: >
  Unified core engine for the CKGroup Adaptive UI Platform.
  Includes design tokens, theming, branding, adaptive components,
  dashboard framework, and plugin system.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  test: ^1.24.0
