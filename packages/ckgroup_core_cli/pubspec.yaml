name: ckgroup_core_cli
description: CLI tool for the CKGroup adaptive UI platform. Adds pages and roles to the runtime page registry.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  ckgroup-core: ckgroup_core
