name: adaptive_components
description: Adaptive, token-driven UI components for the adaptive UI platform.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core_engine:
    path: ../core_engine
  branding_engine:
    path: ../branding_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
