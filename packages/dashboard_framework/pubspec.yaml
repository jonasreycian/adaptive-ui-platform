name: dashboard_framework
description: Dashboard shell, layouts, grid, role-based presets, and plugin module system.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core_engine:
    path: ../core_engine
  branding_engine:
    path: ../branding_engine
  adaptive_components:
    path: ../adaptive_components

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
